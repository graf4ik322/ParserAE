# ==============================================
# Perfume Consultant Bot v2.0 - Docker Config
# ==============================================

# Telegram Bot Configuration
BOT_TOKEN=8377061776:AAHSLR8D7w9gbQbSz7gyO73nb_ywYtozJlY
ADMIN_USER_ID=6398821843

# OpenRouter API Configuration
OPENROUTER_API_KEY=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
OPENROUTER_MODEL=deepseek/deepseek-chat-v3-0324:free
OPENROUTER_MAX_TOKENS=1500
OPENROUTER_TEMPERATURE=0.7

# API Settings
API_MAX_RETRIES=3
API_RETRY_DELAY=1
API_COOLDOWN_SECONDS=30

# Application Settings
ENVIRONMENT=production
LOG_LEVEL=INFO
UPDATE_INTERVAL_HOURS=24
CACHE_EXPIRY_DAYS=7

# Parser Settings
PARSER_MAX_CONCURRENT=10
PARSER_REQUEST_DELAY=1.0
PARSER_TIMEOUT=30
PARSER_RETRY_ATTEMPTS=3

# Data Settings
PERFUME_CATALOG_URL=https://aroma-euro.ru/sitemap.xml

# Database Settings (для профиля with-postgres)
POSTGRES_DB=perfume_bot
POSTGRES_USER=perfume_user
POSTGRES_PASSWORD=secure_password_here

# Monitoring Settings (для профиля monitoring)
GRAFANA_PASSWORD=admin_password_here

# ==============================================
# Docker Compose Profiles:
# 
# Базовый запуск:
# docker-compose -f docker-compose.v2.yml up
#
# С Redis кэшем:
# docker-compose -f docker-compose.v2.yml --profile with-redis up
#
# С PostgreSQL:
# docker-compose -f docker-compose.v2.yml --profile with-postgres up
#
# С мониторингом:
# docker-compose -f docker-compose.v2.yml --profile monitoring up
#
# Полная конфигурация:
# docker-compose -f docker-compose.v2.yml --profile with-redis --profile with-postgres --profile monitoring up
# ==============================================